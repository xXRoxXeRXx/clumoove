// Replaced by the production nginx entrypoint. Leaving this empty lets Vite
// development builds use VITE_API_URL without a failed script request.
window.__CLUMOOVE_RUNTIME_CONFIG__ = Object.freeze({});
