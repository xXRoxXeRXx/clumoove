version: '3.8'

networks:
  migration-network:
    driver: bridge

services:
  # Persistent Database
  postgres-db:
    image: postgres:15-alpine
    container_name: migration-postgres
    restart: unless-stopped
    # Do NOT expose PostgreSQL to the host network. Only reachable from
    # within the Docker network by api-backend and migration-worker.
    environment:
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
      POSTGRES_DB: cloud_migration_db
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./db/schema.sql:/docker-entrypoint-initdb.d/schema.sql
    networks:
      - migration-network

  # Message Broker & Queue
  redis-queue:
    image: redis:7-alpine
    container_name: migration-redis
    restart: unless-stopped
    # Do NOT expose Redis to the host network. It must only be reachable from
    # inside the Docker network. Binding the port publicly allowed the SLAVEOF
    # attack that wiped the queue on 2026-07-08.
    # REDIS_PASSWORD is REQUIRED. The api-backend/worker refuse to start with
    # an empty or known-default password (queue.NewQueue). Do not ship a
    # fallback default here — generate a strong unique password per deployment.
    command: redis-server --appendonly yes --requirepass "${REDIS_PASSWORD}" --bind 0.0.0.0
    volumes:
      - redis_data:/data
    networks:
      - migration-network

  # Go API Gateway & Backend Control
  api-backend:
    image: ghcr.io/xxroxxerxx/clumoove-api:0.9.0
    container_name: migration-api
    restart: unless-stopped
    ports:
      - "8001:8000"
    environment:
      - DATABASE_URL=postgres://${DB_USER:-postgres}:${DB_PASSWORD:-postgres}@postgres-db:5432/cloud_migration_db?sslmode=disable
      - REDIS_URL=redis://:${REDIS_PASSWORD}@redis-queue:6379
      - ENCRYPTION_SECRET_KEY=${ENCRYPTION_SECRET_KEY}
      - JWT_SECRET_KEY=${JWT_SECRET_KEY}
      - CORS_ALLOWED_ORIGIN=${CORS_ALLOWED_ORIGIN}
      - ADMIN_EMAIL=${ADMIN_EMAIL}
      - ADMIN_DISPLAY_NAME=${ADMIN_DISPLAY_NAME}
      - PORT=8000
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
      - DROPBOX_CLIENT_ID=${DROPBOX_CLIENT_ID}
      - DROPBOX_CLIENT_SECRET=${DROPBOX_CLIENT_SECRET}
      - OAUTH_REDIRECT_URI=${OAUTH_REDIRECT_URI}
    depends_on:
      - postgres-db
      - redis-queue
    networks:
      - migration-network

  # Core Migration Engine (Worker)
  migration-worker:
    image: ghcr.io/xxroxxerxx/clumoove-worker:0.9.0
    container_name: migration-worker-1
    restart: unless-stopped
    environment:
      - DATABASE_URL=postgres://${DB_USER:-postgres}:${DB_PASSWORD:-postgres}@postgres-db:5432/cloud_migration_db?sslmode=disable
      - REDIS_URL=redis://:${REDIS_PASSWORD}@redis-queue:6379
      - ENCRYPTION_SECRET_KEY=${ENCRYPTION_SECRET_KEY}
      - GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
      - GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}
      - DROPBOX_CLIENT_ID=${DROPBOX_CLIENT_ID}
      - DROPBOX_CLIENT_SECRET=${DROPBOX_CLIENT_SECRET}
    depends_on:
      - postgres-db
      - redis-queue
    networks:
      - migration-network

  # Web-Frontend (React SPA)
  frontend:
    image: ghcr.io/xxroxxerxx/clumoove-frontend:0.9.0
    container_name: migration-frontend
    restart: unless-stopped
    ports:
      - "3001:3000"
    environment:
      - VITE_API_URL=http://localhost:8001
      - VITE_ALLOWED_HOSTS=${VITE_ALLOWED_HOSTS}
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    depends_on:
      - api-backend
    networks:
      - migration-network

volumes:
  postgres_data:
  redis_data:
